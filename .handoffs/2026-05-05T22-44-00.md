# Handoff

## Status

CLI feature (yojana/1) is complete and deployed. All 7 tasks done. Release binary installed, systemd service running.

## What's ready to use

```
yojana projects              # list active projects
yojana projects --all        # include paused/archived
yojana projects <slug>       # detail view
yojana tasks <slug>          # list tasks (filterable with --status, --category)
yojana tasks <slug/N>        # task detail with acceptance criteria
```

## Potential next steps

- Color/styling with `owo-colors` (low effort, layer on top of display.rs)
- CLI write commands (create task, update status) — not prioritized yet
- Audit/history view for archived projects — explicitly out of scope for now

## Notes for next session

- The `todo.md` file in the repo root is now stale (feature is done) — can be removed
- The escaped newline issue in task descriptions (literal `\n` in DB) is worked around in display.rs but the root cause is in MCP tool input handling — worth investigating if it bothers you
- vidhi skills are loaded and configured; CLAUDE.md points to yojana as the issue tracker

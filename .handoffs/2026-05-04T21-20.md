# Handoff

## What's done

- Slices 01 (HTTP skeleton + project CRUD) and 02 (task CRUD + sequence numbers) are implemented and tested
- PRD at `.scratch/yojana-v0/PRD.md`, 8 issues at `.scratch/yojana-v0/issues/`
- mp-skills agent setup complete (`CLAUDE.md`, `docs/agents/*`)
- Yojana is its own git repo, separated from the manas monorepo

## Pick up next

- **Slice 03 (state machine)** and **slice 04 (edges + cycle detection)** are parallel — both depend on slice 02, not on each other
- Slice 03: status transition validation, history recording. Pure module, no DB changes needed beyond what exists
- Slice 04: `task_edges` table (migration 0003), graph engine module with cycle detection, `yojana_edge` MCP tool
- After both: slice 05 (query + ready detection) depends on 03 + 04

## Context needed

- Manas Rust patterns are documented in the codebase — follow existing rmcp/axum/rusqlite patterns in `src/`
- The `parking_lot::Mutex` on `Db.conn` means task methods must not call other Db public methods (would deadlock) — use free functions that take `&Connection` internally
- Status transitions not yet validated (slice 03 adds this) — currently `update_task` accepts any status string
